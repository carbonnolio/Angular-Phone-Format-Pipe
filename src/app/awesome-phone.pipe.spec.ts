import { AwesomePhonePipe } from './awesome-phone.pipe';

describe('AwesomePhonePipe', () => {
  it('create an instance', () => {
    const pipe = new AwesomePhonePipe();
    expect(pipe).toBeTruthy();
  });
});
